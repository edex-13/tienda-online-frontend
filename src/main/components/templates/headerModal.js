const HeaderModal = (title)=>{
  return `
    <div class="modal-header">
      <h2 class="title-modal">${title}</h2>
      <button class="btn-transition close_btn close-modal"></button>
    </div>
  `
}
export default HeaderModal;